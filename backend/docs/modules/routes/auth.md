# Module: routes/auth.py

## Purpose
Placeholder for future JWT-based authentication. Currently empty.

## Current State
- No routes are implemented; admin login currently lives in `routes/admin.py`.
- `python-jose` is present in dependencies but unused.

## Future Work Ideas
- Implement user signup/login with password hashing.
- Issue JWT access/refresh tokens with expiration.
- Protect chat/resource endpoints per-user if needed.
