# LIFELINE Backend Dependencies
# Updated to include auth, encryption, and AI requirements

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0          # async SQLite driver (dev)
asyncpg==0.29.0            # async PostgreSQL driver (production)
alembic==1.13.1            # database migrations

# Validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Authentication & Security
python-jose[cryptography]==3.3.0   # JWT
PyJWT==2.8.0                       # JWT (alternative, used in auth.py)
passlib[bcrypt]==1.7.4             # password hashing
bcrypt==4.1.3                      # explicit bcrypt

# Encryption
cryptography==42.0.8               # AES-256-GCM via AESGCM

# HTTP client (for OpenRouter API calls)
httpx==0.27.0

# NLP / Text processing
textblob==0.18.0                   # sentiment fallback
nltk==3.8.1                        # text utilities

# Utilities
python-dotenv==1.0.1               # .env loading
python-multipart==0.0.9            # form data

# Rate limiting
slowapi==0.1.9                     # FastAPI rate limiter

# CORS
# (built into FastAPI/Starlette)

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0                      # test client